# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Env file
.env

# Swagger docs (opsional, tergantung instruksi dosen apakah mau di-push atau tidak, 
# tapi umumnya file auto-generated dibiarkan jika project kecil, atau di-ignore)
# docs/
